<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.ViewBindingEx" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@color/black</item>
        <item name="android:navigationBarColor">@color/transparent</item>
        <item name="android:windowLightStatusBar">false</item> <!-- 상태바 아이콘을 어둡게 (밝은 배경용) -->
    </style>


    <style name="Theme.ViewBindingEx" parent="Base.Theme.ViewBindingEx" />
</resources>